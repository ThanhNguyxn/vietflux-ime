
  # VietFlux Tray Popover UI

  This is a code bundle for VietFlux Tray Popover UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  