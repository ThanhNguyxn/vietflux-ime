
  # VietFlux Tray Popover UI

  This is a code bundle for VietFlux Tray Popover UI. The original project is available at https://www.figma.com/design/58ZM2LsZFTWz2DnBepI36O/VietFlux-Tray-Popover-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  